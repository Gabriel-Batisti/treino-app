import { FormCardio } from "./form-cardio";

/**
 * Fora do grupo `(tabs)`: é uma tarefa com começo e fim, e a barra de abas
 * embaixo competiria com o botão de salvar na zona do polegar.
 */
export default function NovoCardio() {
  return <FormCardio />;
}
