import { FormBioimpedancia } from "./form-bioimpedancia";

/**
 * Fora do grupo `(tabs)`: é uma tarefa com começo e fim, e a barra de abas
 * competiria com o botão de salvar na zona do polegar.
 */
export default function Bioimpedancia() {
  return <FormBioimpedancia />;
}
